import { NdaValidators } from './validators';

describe('Validators', () => {

  describe('requiredIf', () => {

    it('should create', () => {
      expect(NdaValidators.requiredIf).toBeTruthy();
    })

  });

});